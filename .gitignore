# revisao: video cru, frames de inspecao, provas. Nunca publicado, nunca versionado.
review/

# saidas de empacotamento
*.zip
site.zip

# crus de geracao, que sao grandes e sao descartaveis
raw*.mp4
*-raw.mp4
*.png.raw

# ruido do sistema
Thumbs.db
desktop.ini
.DS_Store

# O video do hero. Enquanto o arquivo em assets for o clipe sintetico de teste,
# esta linha impede que ele va parar no repositorio. REMOVER esta linha no dia
# em que o video real for aprovado, porque o GitHub Pages so serve o que esta
# versionado.
docs/assets/hero-scrub.mp4
